(ns jank-glfw-test.main)

(cpp/raw "
  #define GLFW_DLL
  #include <glfw3.h>
")

(defn -main [& args]
  (when (cpp/== (cpp/glfwInit) cpp/GLFW_FALSE)
    (throw (ex-info "Error: failed to init glfw\n" {})))
  (println "Hello, world!")
  (cpp/glfwTerminate))